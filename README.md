# 📊 Estudos de SQL

Repositório dedicado ao aprendizado de consultas em bancos de dados.

| Comando | O que faz? | Exemplo |
| :--- | :--- | :--- |
| **SELECT** | Seleciona colunas específicas | `SELECT nome FROM usuarios;` |
| **SELECT *** | Seleciona todas as colunas | `SELECT * FROM usuarios;` |
| **WHERE (Número)** | Filtra resultados numéricos | `SELECT * FROM usuarios WHERE idade > 18;` |
| **WHERE (Texto)** | Filtra resultados de texto | `SELECT nome FROM usuarios WHERE cidade = 'São Paulo';` |
| **ORDER BY** | Ordena de A a Z ou do menor para o maior | `SELECT * FROM usuarios ORDER BY nome;` |
| **ORDER BY DESC** | Ordena do maior para o menor (Decrescente) | `SELECT * FROM usuarios ORDER BY idade DESC;` |

---
*Praticando filtros, seleções e organização de dados.*
